/**
 * Created by eugeneseah on 9/3/17.
 */
const FloatingMenu = (function () {
    let _parent;
    const FloatingMenu = function () {
        _parent = new cc.Node();
        GameView.addView(_parent);


    };
}());